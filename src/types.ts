// Re-export core types for backward compatibility
export type {
	UsageStatsSettings,
	TimeEntry,
	DailyStats,
	TrackingState,
	TrackingSession,
	ChartType,
	ViewPeriod,
	CategoryStats,
	FileStats,
	TagStats,
	ChartData,
	ChartDataset,
} from "./core/types";

export { DEFAULT_SETTINGS } from "./core/types";
